{
  "name": "cloudtrips",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./public/styles.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}