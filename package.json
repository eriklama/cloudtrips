{
  "name": "cloudtrips",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./public/output.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
